// Stub สำหรับ @mediapipe/pose - เราใช้ tfjs runtime ไม่ได้ใช้ MediaPipe จริง
// แต่ @tensorflow-models/pose-detection มี static import 'Pose' ที่ต้อง resolve ได้
export const Pose = class {};
export default { Pose };
